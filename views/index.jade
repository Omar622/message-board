extends layout

block content
  h1= title
  each message in messages
    div
      p user: #{message.user}
      p message: #{message.text}
      p Date: #{message.added}
      hr
  button(onClick = "window.location.replace(window.location.origin+'/new')") Add message